{
  "name": "github-actions-get-action-runs-using-version",
  "version": "1.0.0",
  "description": "Get the GitHub Action runs.using version from the Action metadata file",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build src/index.js -o dist --source-map --license licenses.txt"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zgosalvez/github-actions-ensure-sha-pinned-actions.git"
  },
  "keywords": [
    "actions",
    "node"
  ],
  "author": "Zennon Gosalvez",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "yaml": "^2.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0",
    "eslint": "^8.31.0"
  }
}
